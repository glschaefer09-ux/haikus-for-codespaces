# Logs
logs
*.log
npm-debug.log*

# Dependency directories
node_modules
jspm_packages

# Optional npm cache directory
.npm

# electron-vite build output
out

# electron-builder installer output
release

# Generated app icons (regenerate via `npm run icons:generate`)
build/icon.ico
build/icons

# Environment variables (Supabase project config for Cross PC Sync)
.env
.env.local

# Local per-device app data when running from source
*.local

# OS/editor
.DS_Store
Thumbs.db

# Local AI coding tool state (e.g. Kilo Code worktrees) — not part of the app
.kilo

# TypeScript incremental build cache
*.tsbuildinfo
